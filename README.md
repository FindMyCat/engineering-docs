# Engineering Documentation for FindMyCat

Live at: www.findmycat.io/docs

## Local Development

First, run `pnpm i` to install the dependencies.

Then, run `pnpm dev` to start the development server and visit localhost:3000.
